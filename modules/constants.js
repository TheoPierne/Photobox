
/**
 * URL de l'API.
 * @type {String}
 */
export const API_ENDPOINT = 'https://webetu.iutnc.univ-lorraine.fr/www/canals5/phox/api';

/**
 * URL de base.
 * @type {String}
 */
export const BASE_ENDPOINT = 'https://webetu.iutnc.univ-lorraine.fr';

/**
 * @typedef {Object} ReqOptions
 * @property {string} credentials Credentials à inclure dans la requête.
 */

/**
 * Option(s) pour effectuer une requête.
 * @type {ReqOptions}
 */
export const REQ_OPTIONS = {
	credentials: 'include'
};
